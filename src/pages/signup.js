import { GuestLayout } from '@/layout/GuestLayout';

const SignupPage = () => {
  return <>Signup page</>;
};

SignupPage.getLayout = (page) => <GuestLayout>{page}</GuestLayout>;
export default SignupPage;
